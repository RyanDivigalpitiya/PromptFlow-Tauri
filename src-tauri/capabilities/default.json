{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for every outline window (main + the ⌘N-spawned w* peers)",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
